# OS / editor
.DS_Store
*.swp
*~
# TuyaOpen IDE — machine-local devplat bridge (auto-generated)
.tuyaopen/
tuyaopen.project.ini
.vscode/

# AI agent skills / IDE extension content (not app code)
.agents/
.claude/

# Logs / device captures / flash dumps (may contain license region)
serial-debug-*.txt
*.log
.target_logging/
tyutool_read_*.bin

# TuyaOpen IDE tester-program submissions (internal, not for public repo)
BUG-SUBMISSIONS.md

# Local docs build output (if any)
site/

# Build intermediates (keep the published bins, drop the rest of dist)
source/embedded/.build
source/embedded/dist/*/debug/
source/miniapp/node_modules/
source/miniapp/dist/
source/miniapp/.cache/
source/miniapp/package-lock.json
source/miniapp/yarn.lock
# TuyaOpen IDE — machine-local devplat bridge (auto-generated)
.tuyaopen/ide/
